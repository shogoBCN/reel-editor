# reel-editor – pip dependencies
# Install inside conda env angelica-website:
#   conda activate angelica-website
#   pip install -r requirements.txt
# ffmpeg must be on PATH (Homebrew: ffmpeg).

Pillow==12.3.0
numpy==2.5.2
PyYAML==6.0.3
openpyxl==3.1.5
openai-whisper==20250625
